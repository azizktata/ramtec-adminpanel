export type SalesSummary = {
  today: {
    totalAmount: number;
  };
  yesterday: {
    totalAmount: number;
  };
  thisMonth: {
    totalAmount: number;
  };
  lastMonth: {
    totalAmount: number;
  };
  allTime: {
    totalAmount: number;
  };
};
